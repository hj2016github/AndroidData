连续寻找的技巧: 

寻找最小值:min = a[0],if(a[i]<min) min = a[i];
连通器集合的快速并归技巧:     
public int find(int p) {//p是id[]的索引
    while (p != id[p]) p = id[p];//id[]下也保存索引;只要自旋不等(位置与储存索引相等),p = id[p]是则跳到p,指针所指的位置;
    return p;
}
分为两步
1:id[p]取q赋值给p,下次循环时候跳到q(p)的位置,即id[q];然后再次取值修改,再次跳位置...


